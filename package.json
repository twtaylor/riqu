{
  "name": "riqu-server-container",
  "version": "0.0.1",
  "description": "RIQU's grunt project",
  "scripts": {
    "grunt": "grunt",
    "full-build": "cd src/ && npm install && cd ../server && npm install && cd .. && npm install",
    "start": 
  },
  "author": "Thomas Taylor",
  "license": "ISC",
  "dependencies": {
    "browserify": "^16.2.0",
    "grunt": "^1.0.1",
    "grunt-contrib-clean": "^1.1.0",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-contrib-watch": "^1.0.0",
    "grunt-ts": "^6.0.0-beta.17",
    "tsify": "^4.0.0",
    "typescript": "^2.8.3"
  }
}
